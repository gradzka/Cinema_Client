@if (Session["USER_LOGIN"] != null)
{
    using (Html.BeginForm("LogOff", "Account", FormMethod.Post, new { id = "logoutForm", @class = "navbar-right" }))
    {
        @Html.AntiForgeryToken()

        <ul class="nav navbar-nav navbar-right">
            <li>
                @Html.ActionLink("Zarządzaj rezerwacjami", "Index", "Reservation", routeValues: null, htmlAttributes: new { title = "Manage" })
            </li>

                <!--Html.ActionLink("Witaj, " + @Session["NAME"].ToString() + "!", "Index", "Manage", routeValues: null, htmlAttributes: new { title = "Manage" })-->            

            <li>
                <a href="#">Witaj, @Session["NAME"].ToString()!</a>
                <ul>
                    <li style="height:35px;"><a href="#" style="margin-right:10px;">Edytuj Profil</a></li>
                    <li style="height:35px"><a href="#" style="margin-right:10px; vertical-align:central">Usuń konto</a></li>
                    <li style="height:35px"><a href="#" style="margin-right:10px; vertical-align:central">Zmień hasło</a></li>
                </ul>
            </li> 

            <li>
                <a href="javascript:document.getElementById('logoutForm').submit()">Wyloguj</a>
            </li>
        </ul>
    }
}
else
{
    <ul class="nav navbar-nav navbar-right">
        <li>@Html.ActionLink("Stwórz konto", "Register", "Account", routeValues: null, htmlAttributes: new { id = "registerLink" })</li>
        <li>@Html.ActionLink("Zaloguj się", "Login", "Account", routeValues: null, htmlAttributes: new { id = "loginLink" })</li>
    </ul>
}
